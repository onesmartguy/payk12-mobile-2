import { Alert } from 'react-native';
import Sound from 'react-native-sound';

// import noticeSound from './notice.wav';
// import scanErrorSound from './scan_error.wav';
// import successSound from './success.wav';

// Sound.setCategory('Playback');
// const callback = (error: any, sound: any) => {
//   if (error) {
//     Alert.alert('error', error.message);
//     return;
//   }
//   sound.play();
// };
// export const playNotice = () => {
//   const sound: any = new Sound(noticeSound, error => callback(error, sound));
// };
// export const playSuccess = () => {
//   const sound: any = new Sound(successSound, error => callback(error, sound));
// };
// export const playError = () => {
//   const sound: any = new Sound(scanErrorSound, error => callback(error, sound));
// };
